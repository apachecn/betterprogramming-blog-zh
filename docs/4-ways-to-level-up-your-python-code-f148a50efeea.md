# 提升 Python 代码水平的 4 种方法

> 原文：<https://betterprogramming.pub/4-ways-to-level-up-your-python-code-f148a50efeea>

## 避免不必要的 for 循环，更有效地访问字典条目，等等

![](img/9179d58835f0cc99e2f9ec4ad9613f9c.png)

格伦·卡斯滕斯-彼得斯在 [Unsplash](https://unsplash.com?utm_source=medium&utm_medium=referral) 上拍摄的照片。

Python 的一个好处是很容易让东西工作起来。

另一件好事是 Python 中有许多隐藏的宝藏，让你的代码更干净、更易读，让*更容易编写*。

在本文中，您将学习四种简单的方法来提高您的 Python 水平，并将您从初学者区分为新手！

# 1.用 enumerate()替换 range(len())

有时候，您可能需要迭代一个集合，并跟踪您正在访问的索引。为此，许多人转向迭代`range(len(collection))`。

使用`enumerate()`函数是一种更优雅、更简洁的方式。

`enumerate`函数返回一个`enumerate`对象，存储每一项的索引和值。

这可以让你更好地跟踪你在收藏中的位置，以及它目前的价值。

让我们来看一个用`range(len(collection))`和`enumerate(collection)`方法说明这一点的例子:

另外，如果您想让您的`enumerate`对象从`0`以外的地方开始，您可以简单地将您的起始值传递给`start=`参数。假设我们希望从`1`开始。你可以简单地写:

# 2.停止使用方括号来获取词典条目，使用。获取()

假设你有一本这样的字典:

你想得到`age`的值。你可能习惯写`nik['age']`。虽然这在本例中有效，但是当值不存在时就不起作用了。

例如，试图访问一个不存在的值`'location'`将引发一个`KeyError`:

如果你正在遍历一个字典列表，这会打断你的脚本。当然，您可以使用`try, except`，但是当您可以使用`.get()`方法代替时，为什么要这样做呢？

如果丢失了一个键，该方法将简单地返回`None`,并继续它的快乐之路！

# 3.用 Zip()简化多个列表的迭代

可能有很多次，当您被给予多个集合(比如列表)时，您需要一起迭代每个集合。

像示例 1 一样，您可以使用`range(len(collection))`方式进行，但是很难阅读。

例如，让我们探索以下示例:

这里，我们有一个元组列表，包含每个列表中的项目。

这是一种不错的方式，但是根据你要迭代多少项，这可能会变得非常麻烦。

使用 Python 的内置`zip()`函数，我们可以将它简化很多。

*注意:* `*zip()*` *函数返回一个* `*zip*` *对象，但是您可以直接使用* `*list()*` *、*、*或`f"variable_name = {variable_name}"`、`*dict()*`或*函数将其强制转换为不同的数据类型。**

让我们看看这个例子，并再次返回元组列表:

这写起来更干净！

您也可以直接将东西强制输入字典，但最多只能使用两个集合！

例如，如果您想要一个关键字:值对为 name:age 的字典，您可以写:

使用两个以上的系列将会产生`ValueError`。

# 4.使用 f-strings 轻松打印到控制台

从 Python 3.8 开始，`f`-字符串也可以用来使用`=`字符对代码进行自记录。

这实际上意味着，当您向控制台打印变量值时，您不再需要编写`f"variable_name = {variable_name}"`。

让我们看一个简单的例子:

相反，您可以简单地写道:

这是一个微妙而微小的变化，但它让你的代码更容易阅读，当然也让你免于重复，否则你会发现自己参与其中！

# 结论

这四个技巧可以帮助您的代码看起来更复杂，并避免可能无意中破坏代码的陷阱。

非常感谢你阅读这篇文章！如果你有任何其他的建议，请在评论中告诉我！