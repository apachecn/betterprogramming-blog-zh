# 从 AWS 到 Python 的 3 种自然语言处理工具

> 原文：<https://betterprogramming.pub/3-natural-language-processing-tools-from-aws-to-python-954dbb34b189>

## 使用云解析合同、文章和其他文档

![](img/f6d537cb8d250f759cdaf694e7a28c57.png)

照片由[埃里克·克鲁尔](https://unsplash.com/@ekrull?utm_source=unsplash&utm_medium=referral&utm_content=creditCopyText)在 [Unsplash](https://unsplash.com/s/photos/robot?utm_source=unsplash&utm_medium=referral&utm_content=creditCopyText) 拍摄。

解析和处理文档可以为公司的几乎每个部门提供大量价值。这是自然语言处理(NLP)可以派上用场的众多用例之一。

NLP 不仅仅是聊天机器人和 Gmail 预测你要在邮件中写什么。NLP 还可以用来帮助自动分解、分类和分析文档。例如，也许你的公司正在通过你所有的合同寻找关系，或者你正试图对博客文章或电影剧本进行分类。

这就是使用某种形式的 NLP 处理非常方便的地方。它可以帮助分解文档中的主题、重复主题、代词等等。

现在的问题是，你怎么做？

你应该从头开始开发一个定制的神经网络来分解句子、单词、意思、情感等吗？

这可能不是最好的解决方案——至少对你最初的 MVP 来说不是。相反，有很多库和云服务可以用来帮助分解文档。

在本文中，我们将探讨三个选项，以及如何使用 Python 实现这些工具来分析文档。我们将研究 AWS 理解、GCP 自然语言和 TextBlob。

# AWS 理解

[AWS understand](https://aws.amazon.com/comprehend/)是 AWS 提供的众多云服务之一，它允许您的团队利用神经网络和其他模型，而无需构建自己的复杂模型。

在这种情况下，AWS understand 是一个 NLP API，可以使处理文本变得非常容易。

AWS understand 的伟大之处在于它会自动分解文档中涉及的概念，如实体、短语和语法。如果您试图分解文档中引用的事件、组织、人员或产品，实体尤其有用。

有很多 Python 库可以很容易地分解名词、动词和其他词类。然而，这些库并不是为了准确标注这些名词的类别而构建的。

让我们看一个例子。

对于本文中的所有代码示例，我们将使用下面的文本:

我们将采用这个文本示例，并在下面的代码中运行它，其中变量`plain-text`是:

## AWS 理解输出

一旦您运行上面的代码，您将得到如下所示的输出。这是一个缩短的版本。但是，您仍然可以看到输出。例如，您可以看到`QUANTITY`标有 30 分钟和 5 年——两者都是时间量:

正如你所看到的，AWS understand 在分解组织和其他实体方面做得很好。同样，它不仅限于分解实体。然而，当试图寻找文档之间的关系时，这个特性是更有用的特性之一。

# GCP 自然语言

谷歌已经创建了一个非常相似的 NLP 云服务，叫做[云自然语言](https://cloud.google.com/natural-language)。

它提供了许多类似的功能，包括实体检测、自定义实体检测、内容分类等等。

让我们使用 GCP 版本的自然语言处理字符串。下面的代码显示了一个使用 GCP 检测实体的示例:

## GCP 自然语言输出

GCP 的输出类似于 AWS 的理解。然而，你会注意到 GCP 也分解相似的单词，并试图找到与原始单词相关的元数据:

# TextBlob 和 Python

除了使用云服务提供商，还有一些库也可以从文档中提取信息。特别是 Python 中的 [TextBlob 库](https://textblob.readthedocs.io/en/dev/)非常有用。就我个人而言，这是我学习开发 NLP 管道的第一个库。

它远非完美。然而，它在解析文档方面做得很好。

它提供词类解析，如 AWS 理解和 GCP 自然语言以及情感分析。然而，就其本身而言，它不会对存在的实体进行分类。

它仍然是分解基本单词类型的一个很好的工具。

使用这个库，开发人员可以分解动词、名词或其他词类，然后寻找模式。有哪些常用词？哪些具体的短语或单词在吸引读者？哪些词和其他名词通用？

根据你的最终目标，你仍然可以回答很多问题，开发很多产品。

实现 TextBlob 库非常简单。

在这种情况下，不需要连接到 API。您需要做的就是导入库并调用几个类。

这显示在下面的代码中:

## TextBlob 输出

下面是 TextBlob 的输出。你会看到很多相似的词被自动气象站和 GCP 同时提取出来。然而，API 中并没有额外的标签和元数据。这就是你购买 AWS 和 GCP 的原因(以及其他一些有用的功能):

至此，我们已经介绍了在文档或原始文本中使用 NLP 的三种不同方式。

# 自然语言处理并不一定很难——在某种程度上

NLP 是一个很好的工具，可以帮助处理文档，对它们进行分类，并寻找它们之间的关系。多亏了 AWS 和 GCP，许多不太懂技术的开发人员可以利用一些 NLP 特性。

也就是说，NLP 有很多困难的方面。例如，开发擅长跟踪对话和上下文的聊天机器人并不是一件容易的事情。事实上，在 Medium 上有一个很棒的系列节目，亚当·盖特吉(T1)就讲述了这一点。您可以在文章[中了解更多自然语言处理的乐趣。](https://medium.com/@ageitgey/natural-language-processing-is-fun-9a0bff37854e)

无论你的下一个 NLP 项目是什么，祝你好运。