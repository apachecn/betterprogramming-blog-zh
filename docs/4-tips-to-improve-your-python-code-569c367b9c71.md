# 改进 Python 代码的 4 个技巧

> 原文：<https://betterprogramming.pub/4-tips-to-improve-your-python-code-569c367b9c71>

## 分配多个项目，改善你的条件，等等！

![](img/b882a50bb16f75dbfb4b60a4156efcbb.png)

照片由[丹尼尔·伊德里](https://unsplash.com/@ricaros?utm_source=medium&utm_medium=referral)在 [Unsplash](https://unsplash.com?utm_source=medium&utm_medium=referral) 上拍摄

Python 是一种很棒的编程语言，它使启动和运行变得很容易。这门语言中有许多隐藏的瑰宝，使得它更容易使用。

下面列出的提示不仅使您的代码更容易阅读，而且使您的代码运行起来更安全。你会遇到更少的崩溃和代码中更少的冗余，这意味着需要维护的更少。

# 1.直接分配多个项目

Python 中令人惊叹的特性之一是能够直接分配多个项目。许多 Python 程序员经常没有充分利用多任务这一特性，但它确实可以加快您的工作流程。

术语多重赋值通常可以与元组解包或可迭代解包互换使用。

让我们看看如何直接分配多个项目:

轻松完成多项任务！

这种方法的一个优点是它也适用于解包可迭代的项，比如列表或元组。

让我们看看如何将列表的各个部分分配给不同的变量:

清单拆包很好玩！

这里需要注意的一点是，变量的数量应该是相同的。

例如，如果你写了`a, b = [1,2,3]`，那么这将抛出一个`ValueError`。出现这种情况的原因是任务的两边不匹配。

要解决这个问题，您可以使用`*`操作符*解包*剩余的值:

解包多个变量。

# 2.更好的链式条件逻辑

Python 还为您提供了编写条件表达式的灵活性。

看看下面的例子。你可能想评估一个数大于某个数小于另一个数。在这种情况下，您可以写:

较长条件句的一个例子

您可以通过编写以下代码来使您的代码更容易阅读，而不是这样写:

一种更数学化和更简短的评估条件的方法。

# 3.将两个字典合并成一行(Python 3.9+)

Python 3.9 使得合并两个字典变得非常容易。在 3.9 版本之前，您需要将字典条目合并到另一个条目中，以便合并它们。

现在您可以简单地使用 union `|`操作符来帮助合并字典:

Python 3.9 中更好的字典合并

# 4.在一行中编写 if-else 语句

Python `if-else`语句是很好的资源，可以让你在代码中编写条件表达式。

通常，这些语句看起来像这样:

传统的 if-else

我们可以看到这是相当清楚的，但它也占用了四行。

在 Python 中，我们实际上可以在一行条件赋值语句中表达这一整行。

让我们来看看这个是什么样子的:

Python 中的单行 if-else 赋值

# 结论

我希望这四个技巧有助于使您的代码更快、更容易编写(和阅读！).Python 以其灵活性而闻名，我希望这四颗隐藏的宝石能让你成为一名更高效的程序员。

非常感谢你阅读这篇文章！如果你有任何问题或意见，请在评论中告诉我！