# TypeScript:匿名类型、类型别名和接口声明

> 原文：<https://betterprogramming.pub/typescript-anonymous-types-type-aliases-and-interface-declarations-b60bc8a08f8c>

## 关于类型定义的完整指南

![](img/eb5c6a288a5f5441a5b410e3215900e6.png)

照片由[莎伦·麦卡琴](https://unsplash.com/@sharonmccutcheon?utm_source=medium&utm_medium=referral)在 [Unsplash](https://unsplash.com?utm_source=medium&utm_medium=referral) 上拍摄

TypeScript 是一种基于 JavaScript 的强类型编程语言。它在编译期间提供类型检查，并且大多数 ide 都支持在我们键入代码时进行类型检查。TypeScript 是为开发大型应用程序而设计的。TypeScript 代码被反编译成在浏览器中运行的 JavaScript。

类型安全在 TypeScript 中至关重要。它阻止或防止由不同数据类型之间的差异导致的类型错误。定义类型有三种方式:

*   匿名类型:它们是没有名字的类型，包含属性和方法。它通常指定义了一组相关属性和方法的对象类型。
*   类型别名:它们是现有类型或新创建类型的替代名称。它适用于基本类型、对象类型等等。可能就简单叫`type`。
*   接口声明:它们是新创建类型的名称。它指的是对象类型，其中定义了一组相关的属性和方法。或许可以简称为`interface`。

让我们仔细看看这些类型。

# 匿名类型是临时的

TypeScript 是强类型的。变量、参数或属性的类型可以用名称后的`:Type`来指定。冒号后面可能有一个或多个空格。`Type`包括所有有效类型，可能是基元类型、匿名类型、类型别名或接口声明。

匿名类型是没有任何名称但包含属性和方法的类型。下面是匿名类型的一个示例:

`grow`(第 1-4 行)和`speak`(第 10 行)的函数参数由匿名类型定义。这是抑制打字稿投诉的一种快速而肮脏的方法，但这种方法是临时的，很难维护。

当类型包含属性和方法时，建议不要使用匿名类型。相反，类型别名或接口声明应该是更好的选择。

# 类型别名和接口声明具有不同的语法

类型别名是任何类型的名称。语法是`type <name> = <definition>`。下面是一个对象类型定义的示例:

第 1–7 行定义了一种类型的`Person`，它包括属性(第 2–5 行)和方法(第 6 行)。每个属性都有一个类型名，可以标记为`readonly`(第 2 行)，或者可选的(第 4 行的`?`)。有两种方法来定义函数，作为箭头函数的属性(第 5 行)或作为普通函数的方法(第 6 行)。

第 9–17 行定义了`Person`类型的`record`。

第 19 行打印出记录内容。

第 27 行调用属性定义的函数`speak1`。第 28 行调用方法定义的函数`speak2`。他们俩的工作方式是一样的。选择哪种方式，看个人喜好。

接口声明定义了一组描述对象的相关属性和方法。语法是`interface <name> { <definition> }`。

对于前面的例子，我们将`type`改为`interface`(第 1 行)，其工作方式都是一样的。

对于定义对象，类型别名和接口声明非常相似，在大多数情况下，我们可以在它们之间自由选择。

# 类型别名适用于任何类型

类型别名可以定义或重命名任何类型。适用于原语类型、对象类型、[枚举类型、](/a-complete-guide-to-enums-in-typescript-6f76da009146)联合类型、元组类型、交集类型、`keyof`类型、`typeof`类型、模板文字类型、索引访问类型、映射类型、条件类型、接口类型、类类型、泛型类型等。

接口声明只能定义对象的形状，尽管每个接口的属性/方法都可以由任何有效类型定义，包括类型别名和接口声明。

这里有一个建立在前面的`type`例子上的`interface`:

# 类型别名和接口声明都可以扩展

类型别名和接口声明都可以扩展，尽管语法不同。类型别名`extend` 由交集完成，接口声明`extend`使用内置的`extend`机制。它们都可以从一个或多个接口、类型和/或类中扩展。

# 类可以实现接口和类型

在大多数情况下，一个类可以实现一个或多个接口和/或类型。我们将`WaterAnimal`定义为`interface`，将`FlyingAnimal`定义为`type`。一个类可以实现其中任何一个。

一个类被认为是静态的蓝图。它可以实现任何接口，这也是一个静态蓝图。但是，类不能实现命名联合类型的类型别名。

在上面的代码中，第 3 行显示了一个错误:

```
A class can only implement an object type or intersection of object types with statically known members.
```

# 接口声明可以有声明合并

声明合并意味着编译器将两个用相同名称声明的独立声明合并成一个定义。合并的定义具有两个原始声明的特征。事实上，它并不局限于两个声明。可以合并任意数量的声明。

以下是第一个接口(第 1–5 行)和第二个接口(第 7–9 行)合并成一个组合接口的示例:

声明合并不适用于类型别名。如果我们将第 1 行和/或第 7 行更改为`type`，则显示错误:`Duplicate identifier 'Person'.`

在某种程度上，类型别名使用交集实现了类似的功能。

# 结论

我们已经走过了类型定义。匿名类型是临时的，不推荐使用。类型别名适用于任何类型，接口声明用于对象。对于定义对象，它们非常相似，我们可以在它们之间自由选择，只是类不能实现命名联合类型的类型别名。

感谢阅读。我希望这有所帮助。如果你有兴趣，可以看看[我的其他媒体文章](https://jenniferfubook.medium.com/jennifer-fus-web-development-publications-1a887e4454af)。