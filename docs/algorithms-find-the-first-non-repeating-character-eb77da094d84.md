# 算法:查找第一个不重复的字符

> 原文：<https://betterprogramming.pub/algorithms-find-the-first-non-repeating-character-eb77da094d84>

## 在字符串中搜索第一个唯一字符

![](img/7250760fdf3038a024f8fa170b445541.png)

由[罗马法师](https://unsplash.com/@roman_lazygeek?utm_source=medium&utm_medium=referral)在 [Unsplash](https://unsplash.com?utm_source=medium&utm_medium=referral) 上拍摄的照片。

在本文中，我们将提出一个算法挑战，然后提供两个解决方案:亚军和最佳算法。算法是用 Python 写的。

# 挑战

在一串小写字母中查找第一个不重复的字符。非重复字符被定义为在字符串中只出现一次的字符。如果所有字符都重复，则返回一个下划线。

以下是一些示例字符串和每个字符串的答案:

```
"aabcdb" # c
"abcddc" # a
"aabbcc" # _
"abbcda" # c
```

既然我们理解了挑战，我鼓励你自己去尝试。

如果您已经尝试过，或者您想跳过前面的步骤，那么让我们来看看解决方案吧！

# 亚军

使用一对 for 循环，我们可以以嵌套方式迭代字符串，以确定某个字符是否重复:

关于此解决方案的几点说明:

*   嵌套 for 循环算法的多项式时间复杂度为 *O(n^2)* 。
*   通过将算法简化为单个循环来优化嵌套循环解决方案。

# 最优解

为了避免嵌套循环，我们可以使用字典跟踪每个值在字符串中出现的次数——其中字符是术语，计数是定义。

一旦我们有了计数，字典就成为我们检查第一个只出现一次的字符的参考:

关于此解决方案的一些注意事项:

*   由于字典的定义是无序的，所以我们应该再循环一次，以确保我们保持了字符串的顺序。
*   从 3.7 版本开始，Python 字典将保留条目顺序。这将消除遍历字符串的需要，并允许我们直接遍历字典。

# 奖金解决方案

幸运的是，对于我们 Pythonistas 来说，有一些原生的字符串函数使得这个算法变得轻而易举。

利用`.find()`和`.rfind()`，我们可以确定一个字符左右的索引。如果左搜索和右搜索返回相同的索引，那么我们可以断定该字符只出现一次。

```
def firstNotRepeatingCharacter(my_string):
  for c in my_string:
    if my_string.index(c) == my_string.rindex(c):
      return c
  return "_"
```

关于此解决方案的一些注意事项:

*   由于该算法依赖于特定于语言的方法，因此它不是语言不可知的。
*   虽然该算法在语法上比我们的最优解迭代次数少，但两者的时间复杂度相同。

# 结论

你过得怎么样？你最初的解决方案是什么？下面分享一下你的经历吧！

特别感谢尼克·怀特对这个算法的解释！