# 反应干净的代码

> 原文：<https://betterprogramming.pub/8-ways-to-write-clean-react-code-610c502ccf39>

## 编写更好更干净的代码的简单方法

![](img/fbd8056f9c0d74e1c2a5ad6d0524cd54.png)

[抓拍](https://unsplash.com/@candid?utm_source=medium&utm_medium=referral)在 [Unsplash](https://unsplash.com?utm_source=medium&utm_medium=referral) 上的照片。

干净的代码不仅仅是工作代码。干净的代码易于阅读，易于理解，组织整齐。在这篇文章中，我们将看看八种可以编写更干净的 React 代码的方法。

当浏览这些建议时，重要的是要记住它们就是:建议。如果你不同意他们中的任何一个，那完全没问题。然而，这些实践对我编写自己的 React 代码很有帮助。

让我们开始吧！

# 1.仅针对一个条件的条件呈现

如果您需要在条件为`true`时有条件地呈现某些内容，而在条件为`false`时不呈现任何内容，请不要使用三元运算符。请使用`&&`操作符。

坏例子:

好例子:

# 2.任一条件下的条件渲染

如果您需要在条件为`true`时有条件地呈现一个事物，而在条件为`false`时呈现一个不同的事物，请使用三元运算符。

坏例子:

好例子:

# 3.布尔道具

一个 truthy 属性可以被提供给一个只有属性名而没有如下值的组件:`myTruthyProp`。像`myTruthyProp={true}`这样写就没必要了。

坏例子:

好例子:

# 4.弦乐道具

可以在双引号中提供字符串属性值，而不使用花括号或反斜杠。

坏例子:

好例子:

# 5.事件处理函数

如果一个事件处理程序只接受`Event`对象的一个参数，你可以像这样提供函数作为事件处理程序:`onChange={handleChange}`。不需要像这样把函数包装在匿名函数里:`onChange={e => handleChange(e)}`。

坏例子:

好例子:

# 6.将组件作为道具传递

当将一个组件作为道具传递给另一个组件时，如果该组件不接受任何道具，则不需要将这个传递的组件包装在函数中。

坏例子:

好例子:

# 7.未定义的道具

未定义的道具被排除在外，所以如果道具未定义也没问题，不要担心提供一个`undefined`后备。

坏例子:

好例子:

# 8.设置依赖于前一状态的状态

如果新状态依赖于前一状态，请始终将状态设置为前一状态的函数。React 状态更新可以是批处理的，不这样写更新会导致意想不到的结果。

坏例子:

好例子:

# 荣誉奖

下面的实践不是特定于 React 的，而是用 JavaScript(或任何编程语言，就此而言)编写干净代码的良好实践。

*   将复杂的逻辑提取到明确命名的函数中。
*   将幻数提取为常数。
*   使用明确命名的变量。

编码快乐！