# 你应该知道的 Node.js 中的 4 个创新设计模式

> 原文：<https://betterprogramming.pub/the-4-creational-design-patterns-in-node-js-that-you-should-know-cfb7ed77a84e>

## 了解单例、工厂、构建器和原型模式

![](img/336ae6ab4dfe987152a80f53d3a2bb77.png)

Photo by [贝莉儿 DANIST](https://unsplash.com/@danist07?utm_source=medium&utm_medium=referral) on [Unsplash](https://unsplash.com?utm_source=medium&utm_medium=referral).

首先，我们先来解释一下什么是设计模式。简而言之，它们允许我们重用代码来解决重复出现的问题。我们可以重用已经有效的代码，而不是一次又一次地解决同样的问题。

为什么要使用设计模式？以下是一些很好的理由:

*   它们被很好地记录和测试。
*   它们在许多不同的情况下都是可重用的。
*   他们很有效率。
*   他们会节省你的时间。

值得注意的是，这些模式是通用的，这意味着它们适用于其他编程语言/框架/库。然而，在本文中，我们将使用 Node.js 来实现和理解它们。

有三种类型的设计模式:

1.  创建—对象实例的创建
2.  结构—对象的设计方式
3.  行为——物体如何相互作用

# 创造性的设计模式

在本文中，您将了解创造性的设计模式。他们关心对象实例的创建。在我接下来的文章中，你也会学到结构和行为模式。

本文中涉及的创造性设计模式有:

1.  一个
2.  工厂
3.  建设者
4.  原型

事不宜迟，让我们从单例设计模式开始。

# 一个

从它的名字:*单*吨就可以一窥这种设计模式的用途。当我们只需要一个类的单个实例时，我们使用这种设计模式。这意味着我们不能创建多个实例，只能创建一个。如果没有实例，则会创建一个新实例。如果存在现有实例，它将使用该实例。

让我们看一个实现了单例模式的例子:

正如您所看到的，有许多样板文件来实现 Singleton。因此，让我们看看实现这种设计模式的一种更短的方法。解决方案是完全删除`Singleton`类，并导出类`DatabaseConnection`的一个新实例。

为什么会这样？在 Node 中，它的工作是因为模块缓存系统。根据[的文档](https://nodejs.org/api/modules.html#modules_caching)，“模块在第一次加载后被缓存。”在上面的第二个例子中，导出的新实例被缓存，并在每次需要时重用。

因此，这是在 Node 中实现单例模式的两种方式。

概括一下:

*   当你只需要一个类的实例时，Singleton 是很有用的。
*   在 Node 中，我们可以利用模块缓存系统直接导出实例。

# 工厂

再一次，我们可以通过查看它的名字来了解这种设计模式的作用。工厂设计模式允许我们定义用于创建对象的接口或抽象类。然后我们使用接口/抽象类来实例化不同的对象。

考虑一个应用程序，其中我们必须创建和使用所有类型的车辆。有机动车(汽车、公共汽车、卡车、摩托车)、轨道交通工具(火车、电车)、飞机(飞机、直升机)和水上交通工具(轮船、小船)。因此，我们可以如下实现工厂模式，而不是通过单独调用每个类的构造函数来创建实例:

您可以使用`VehicleFactory`并指定类型，而不是单独创建每个类的实例。您将创建一个新的`Car`实例，如下所示:

```
**const** audiAllRoad = VehicleFactory('car', 'Audi', 'A6 Allroad', '2020');
```

使用工厂设计模式的好处是它将对象的构造从对象本身中分离出来。您还可以在不破坏现有代码的情况下向应用程序中引入新对象。最后，它帮助您更好地组织代码，因为所有与创建实例相关的代码都在一个地方。

概括一下:

*   工厂模式为创建对象提供了一个接口/抽象类。
*   您可以通过使用相同的接口/抽象类来创建不同的对象。
*   它改进了代码的结构，使其更易于维护。

# 建设者

构建器设计模式允许我们将对象的构造与它们的表示分离开来。因此，它简化了创建复杂对象的代码。对于简单的对象来说，这可能有点过了，但是对于复杂的对象来说，这简化了它们的创建。

为了帮助您理解这种设计模式，我们来看一个例子。假设我们想制造汽车。在这个例子中，我们有`Car`和`CarBuilder`类。

现在我们用`CarBuilder`代替`Car`来制造汽车。我们的做法如下:

使用构建器设计模式使得复杂对象的创建不容易出错，因为您可以很容易地理解每个参数的作用。相比之下，下面是我们如何创建没有`CarBuilder`类的汽车:

```
**const** bmw = **new** CarBuilder('bmw', 'x6', 2020, true, true);
```

你可以看到它是如何变得令人困惑的。那两个布尔值(`true`)是什么意思？现在想象这个物体更复杂。创建对象会令人困惑，并且引入错误的几率会更高。

因此，构建器设计模式对于分离复杂对象的创建和表示非常有用。

# 原型

JavaScript 是一种基于原型的语言，这意味着它使用原型继承。这意味着每个对象都继承自其他对象。

因此，我们通过克隆原型的值来创建新的对象，这也可以称为样本对象。这意味着原型充当新对象的蓝图。使用这种设计模式的一个重要好处是对象中定义的函数是通过引用创建的。这意味着所有对象都指向同一个函数，而不是拥有该函数的副本。更简单地说，原型的功能可用于从原型继承的所有对象。

好了，说够了。让我们看一个例子，这样你可以更好地理解这个设计模式是关于什么的。我们将继续汽车场景。

为了从原型创建一个新的对象，我们可以使用`Object.create()`方法。第二个对象(`secondATV`)的值与第一个对象(`atv`)的值相同。为了确保它正常工作，请尝试调用`mud`函数或打印任何对象属性。

使用原型设计模式的另一种方式是在“类”中指定原型你可以看到下面的例子:

同样，两个实例都可以访问在`atvPrototype`对象中定义的任何内容。而且，它们指向同一个函数，而不是拥有它们的`mud`函数。

总之，原型设计模式非常有用——尤其是当您希望对象共享相同的功能或属性时。它还充当所有新对象的蓝图。

概括一下:

*   JavaScript 是一种基于原型的语言。
*   它使用原型继承。
*   每个对象都继承自其他对象。
*   新对象是按照一个叫做原型的蓝图创建的。
*   原型上定义的函数被所有新类继承。
*   新的类指向同一个函数，而不是有单独的副本。

# 结论

这篇文章是我的#learninpublic 过程的一部分。因此，重要的是要注意，这篇文章是一个起点，你当然可以挖掘得更深。如果你想深入了解，我推荐《设计模式:可重用面向对象软件的元素》这本书。

感谢阅读！

> [学习 Node.js](https://rli.to/PhkSl) ，从零开始构建高级大型应用&被聘为后端开发人员